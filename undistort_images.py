    #transformer = transformers[args.lens]()
    #images_with_corners = calib.draw_corners(images)
    #save_images(os.path.abspath("data/testin"), images_with_corners, f"{lens}_corners")
    #transformed_img = []
    #for image in images:
    #    img = transformer.undistort(image, cam)
    #    transformed_img.append(img)
    #save_images(os.path.abspath("data/testin"), transformed_img, f"{lens}_transf")